@{
}

@model BooksViewModel

<h1> Bookstore </h1>

@foreach (Book b in Model.Books)
{
    <div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            <h4>
                @b.Title
                <span class="badge badge-pill badge-primary" style="float:right; color: black;">
                    <small>@b.Price.ToString("c")</small>
                </span>
            </h4>
        </div>
        <div class="card-text p-1">
            <ul>
                <li>@b.Author</li>
                <li>@b.Publisher</li>
                <li>@b.Isbn</li>
                <li>@b.PageCount</li>
            </ul>
        </div>
    </div>

    @*<h3>@b.Title</h3>
        <ul>
            <li>@b.Author</li>
            <li>@b.Publisher</li>
            <li>@b.Isbn</li>
            <li>@b.PageCount</li>
            <li>@b.Price</li>
        </ul>
        <br />*@
}

<div  page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1" page-blah="@Model.PageInfo" page-action="Index"></div>